require 'rails_helper'

describe "User" do
  it "sees a nav bar." do
    # visit root_path

    # expect(page).to have_link(href: beers_path)
    # expect(page).to have_link(href: breweries_path)

  end
end